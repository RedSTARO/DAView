# Build
build/
*/build/
.gradle/
.kotlin/
local.properties
kotlin-js-store/

# IDE
.idea/
*.iml
.vscode/

# Runtime data (contains WebDAV credentials & API keys)
/run/
/data/
server.config.json
*.local.json
*.db
*.db-journal
*.sqlite

# OS
.DS_Store
Thumbs.db

# JVM crash dumps
hs_err_pid*.log
replay_pid*.log

# Dev-server launch config: holds machine-specific absolute paths
.claude/launch.json

# The in-app player's engine. ~115 MB, fetched by scripts/fetch-libmpv rather
# than committed; CI runs that before packaging the desktop app.
/composeApp/nativeResources/*/libmpv*
